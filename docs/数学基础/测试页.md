# 测试页

## 行内公式

向量点积：$\vec{a} \cdot \vec{b} = |a||b|\cos\theta$，导数记号 $f^{\prime}(x) \gt 0$。

## 块级公式

$$
A = U \Sigma V^{T}
$$

$$
R(\alpha) \cdot R(\beta) = R(\alpha + \beta)
$$

## SVG 配图

![测试图](assets/测试.svg)